#include "Cell.h"

//Cell::Cell(bool alive, int type, Vector3d color) : isAlive(alive), type(type), color(color) {}

//void Cell::setAlive(bool alive) {
//    isAlive = alive;
//}
//
//bool Cell::getAlive() const {
//    return isAlive;
//}